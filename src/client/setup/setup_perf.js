define(function (require, exports, module) {
  "use strict";
  // var perf = require("../ext/profiler");
  // perf.profileObject(ace.require("ace/document").Document.prototype);
  // var prop = ace.require("ace/editor").Editor.prototype;
  // prop._emit = perf.profileEvents(prop._emit);
  // perf.profileListeners(prop);
  // prop.addEventListener = prop.on;
  // prop.removeEventListener = prop.removeListener = prop.off;
  // for(var i in window){
  //   if(i==='ondevicemotion' || i==='ondeviceorientation' || i==='ondeviceorientationabsolute')continue;
  //   if(i.startsWith('on')){
  //     window.addEventListener(i.substring(2),console.log.bind(console,i));
  //   }
  // }
  // ace.Editor.prototype.$useTouchScroller = false;
  // ace.Editor.prototype.$enableTouchHandles = false;
  // perf.profileObject(
  //   require("../editor/ace_helpers").ScrollbarMarker.prototype
  // );
  // perf.profileObject(
  //   ace.require("ace/virtual_renderer").VirtualRenderer.prototype
  // );
  // perf.profileObject(ace.require("ace/layer/text").Text.prototype);
  // perf.profileObject(ace.EditSession.prototype);
  // perf.profileObject(ace.Editor.prototype);
});